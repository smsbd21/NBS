@model NBS.Tb_Area

@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_SidePanel.cshtml";
}

@using (Html.BeginForm("Create", "Area", FormMethod.Post, new { enctype = "multipart/form-data", @class = "form-horizontal", role = "form" }))
{
    @Html.AntiForgeryToken()
    <div class="form-horizontal">
        <h3><strong>Create New Area</strong></h3>
        <hr />
        @Html.ValidationSummary(true, "", new { @class = "text-danger" })
        @Html.CheckBoxFor(model => model.Status, new { @checked = "checked", @type = "hidden" })
        <div class="form-group">
            @Html.LabelFor(model => model.Area, "Area Name", new { @class = "control-label col-md-2" })
            <div class="col-md-4">
                @Html.EditorFor(model => model.Area, new { htmlAttributes = new { @class = "form-control" } })
                @Html.ValidationMessageFor(model => model.Area, "", new { @class = "text-danger" })
            </div>
        </div>

        <div class="form-group">
            <div class="col-md-offset-2 col-md-4">
                <button type="submit" class="btn btn-outline-success">
                    <i class="fas fa-plus mr-2"> Create</i>
                </button> &nbsp; | &nbsp;
                @Html.ActionLink("Back to List", "Index")
            </div>
        </div>
    </div>
}
